import { NamedAPIResourceInterface } from "./namedAPIResouce.interface";

//Estructura de datos que contiene el nombre e idioma en el que esta escrito
export interface NameInterface {
    name: string,
    language: NamedAPIResourceInterface
}